using nat;

# Integers: definition

Axiom.  There exists a type ℤ (the integers) with a function z : ℕ ⨯ ℕ → ℤ such that

  a. For all a, b, c, d ∈ ℕ, z(a, b) = z(c, d) if and only if a + d = b + c.  [equality on ℤ]
  b. For all x : ℤ, there exist some a, b : ℕ such that x = z(a, b).

Definition.  Let 0 : ℤ = z(0, 0).  Let 1 : ℤ = z(1, 0).  Let 2 : ℤ = z(2, 0).

Lemma (ℤ zero).  For all a, b ∈ ℕ, z(a, b) = 0 if and only if a = b.

# Addition

Axiom.  There is a binary operation + : ℤ → ℤ → ℤ such that for all n, j, k, i : ℕ,

    z(n, j) + z(k, i) = z(n + k, j + i).

Axiom.  There is a unary operation − : ℤ → ℤ such that for all a, b ∈ ℕ,

  −z(a, b) = z(b, a).

Theorem.  Let a, b, c : ℤ.

  a. a + b = b + a.
  b. a + (b + c) = (a + b) + c.
  c. a + 0 = a.
  d. a + (−a) = 0.

Proof.

  a. There are some n, j, k, i : ℕ such that a = z(n, j) and b = z(k, i).  Then

    a + b = z(n, j) + z(k, i)
          = z(n + k, j + i)
          = z(k + n, i + j)
          = z(k, i) + z(n, j)
          = b + a.

  b. There are some n, j, k, i, m, h ∈ ℕ such that a = z(n, j) and b = z(k, i) and c = z(m, h).  Then
  
    (a + b) + c = (z(n, j) + z(k, i)) + z(m, h)
                    = z(n + k, j + i) + z(m, h)
                    = z((n + k) + m, (j + i) + h)
                    = z(n + (k + m), j + (i + h))
                    = z(n, j) + z(k + m, i + h)
                    = z(n, j) + (z(k, i) + z(m, h))
                    = a + (b + c).

  c. There are some n, j : ℕ such that a = z(n, j).  So

    a + 0 = z(n, j) + z(0, 0)
            = z(n + 0, j + 0)
            = z(n, j)
            = a.

  d. There are some n, j : ℕ such that a = z(n, j).  So

    a + (−a) = z(n, j) + −z(n, j)
                = z(n, j) + z(j, n)
                = z(n + j, j + n)
                = z(0, 0)     by [equality on ℤ]
                = 0.

# Integers: multiplication

Axiom.  There is a binary operation · : ℤ → ℤ → ℤ such that for all n, j, k, i : ℕ,

    z(n, j) · z(k, i) = z(n · k + j · i, j · k + n · i).  [def of ·]

Definition.  For all x : ℤ, x^(2 : ℤ) = x · x.

Theorem.  Let a, b, c : ℤ.

  a. a · b = b · a.
  b. (a · b) · c = a · (b · c).
  c. a · (b + c) = a · b + a · c.  [ℤ left distributive]
  d. (a + b) · c = a · c + b · c.  [ℤ right distributive]
  e. a · 1 = a.

Proof.

  a. There are some n, j, k, i : ℕ such that a = z(n, j) and b = z(k, i).  Then

    a · b = z(n, j) · z(k, i)
            = z(n · k + j · i, j · k + n · i)
            = z(k · n + i · j, i · n + k · j)
            = z(k, i) · z(n, j)
            = b · a.

  b. There are some n, j, k, i, m, h : ℕ such that a = z(n, j) and b = z(k, i) and c = z(m, h).  Then

    (a · b) · c = (z(n, j) · z(k, i)) · z(m, h)
          = z(n · k + j · i, j · k + n · i) · z(m, h)
          = z((n · k + j · i) · m + (j · k + n · i) · h,
                 (j · k + n · i) · m + (n · k + j · i) · h)
          = z(((n · k) · m + (j · i) · m) + ((j · k) · h + (n · i) · h),
                 ((j · k) · m + (n · i) · m) + ((n · k) · h + (j · i) · h)) by [right distributive]
          = z(n · (k · m) + n · (i · h) + j · (i · m) + j · (k · h),
                 j · (k · m) + j · (i · h) + n · (i · m) + n · (k · h))
                  by [associativity of multiplication] and [commutativity of addition]
          = z(n · (k · m + i · h) + j · (i · m + k · h),
                 j · (k · m + i · h) + n · (i · m + k · h))  by [right distributive]
          = z(n, j) · z(k · m + i · h, i · m + k · h)
          = z(n, j) · (z(k, i) · z(m, h))
          = a · (b · c).

  c. There are some n, j, k, i, m, h : ℕ such that a = z(n, j) and b = z(k, i) and c = z(m, h).  Then

      a · (b + c) = z(n, j) · (z(k, i) + z(m, h))
                      = z(n, j) · z(k + m, i + h)
                      = z(n · (k + m) + j · (i + h), j · (k + m) + n · (i + h))
                      = z(n · k + n · m + j · i + j · h,
                          j · k + j · m + n · i + n · h)
                      = z(n · k + j · i,  j · k + n · i) +
                            z(n · m + j · h, j · m + n · h)
                      = z(n, j) · z(k, i) + z(n, j) · z(m, h) by [def of ·]
                      = a · b + a · c.

  d. (a + b) · c = c · (a + b) = c · a + c · b = a · c + b · c.

  e. There are some n, j : ℕ such that a = z(n, j).  Then

        a · 1 = z(n, j) · 1
              = z(n, j) · z(1, 0)
              = z(n, j) by [def of ·]
              = a.
    
Theorem.  Let a, b : ℤ.  If a ≠ 0 and b ≠ 0, then a · b ≠ 0.  [zero product]

Proof.  Assume that a ≠ 0 and b ≠ 0.  There are some n, j, k, i : ℕ such that a = z(n, j) and b = z(k, i).  It follows that n ≠ j and k ≠ i.  Since k ≠ i, by [trichotomy] k < i or i < k.

Suppose that i < k.  Then k = i + s(u) for some u : ℕ.  Since n ≠ j, it follows by [cancellation law for multiplication] that n · s(u) ≠ j · s(u).  Hence

    n · k + j · i = n · (i + s(u)) + j · i
                     = n · i + n · s(u) + j · i
                     ≠ n · i + j · s(u) + j · i by [cancellation law for addition].

But

    n · i + j · s(u) + j · i = j · (i + s(u)) + n · i
                                   = j · k + n · i.

Thus n · k + j · i ≠ j · k + n · i.

Now suppose that k < i.  Then i = k + s(u) for some u : ℕ.  Since n ≠ j, it follows by [cancellation law for multiplication] that n · s(u) ≠ j · s(u).  Hence

    n · k + j · i = n · k + j · (k + s(u))
                     = n · k + j · k + j · s(u)
                     ≠ n · k + j · k + n · s(u) by [cancellation law for addition].

But

    n · k + j · k + n · s(u) = n · (k + s(u)) + j · k
                                      = n · i + j · k.

Thus n · k + j · i ≠ j · k + n · i.

In any case n · k + j · i ≠ j · k + n · i, so

    a · b = z(n · k + j · i, j · k + n · i) ≠ 0.

# Theorems that hold in ℤ and any ring

Theorem.  Let x, y : ℤ.

    x + y = 0 if and only if y = −x.

Proof.  If y = −x, then x + y = 0.  Now suppose that x + y = 0.  Then

    y = 0 + y = (x + −x) + y = (x + y) + −x = 0 + −x = −x.

Theorem.  Let x, y, z : ℤ.

    a. If x + y = x + z then y = z.
    b. −0 = 0.
    c. −(−x) = x.
    d. If −x = −y, then x = y.
    e. −(x + y) = (−x) + (−y).
    f. If x + y = x, then y = 0.    [addend is 0]
    g. x · 0 = 0 = 0 · x.  [multiply by 0]
    h. −(x · y) = (−x) · y = x · (−y).  [multiply by negative]
    i. (−x) · (−y) = x · y.  [mul neg neg]

Proof.

    a. Assume that x + y = x + z.  Then (−x) + x + y = (−x) + x + z.  Hence 0 + y = 0 + z.  Therefore y = z.
    
    c. (−x) + x = x + (−x) = 0.  It follows that −(−x) = x.

    d. Suppose that −x = −y.  Then −(−x) = −(−y), so x = y.

    g. x · 0 = x · (0 + 0) = x · 0 + x · 0, so x · 0 = 0 by [addend is 0].  Similarly, 0 · x = (0 + 0) · x = 0 · x + 0 · x, so 0 · x = 0 by [addend is 0].

    h. x · y + (−x) · y = (x + −x) · y = 0 · y = 0, so −(x · y) = (−x) · y. 
    
    Similarly, x · y + x · (−y) = x · (y + (−y)) = x · 0 = 0, so x · (−y) = −(x · y).

# Subtraction

Definition.  For all x, y : ℤ, x − y = x + (−y).

Theorem.  Let x, y, z : ℤ.

    a. x − x = 0.
    b. x − 0 = x.
    c. 0 − x = −x.
    d. −(x − y) = y − x.
    e. x − y = 0 if and only if x = y.  [subtract to 0 iff equal]
    f. x · (y − z) = x · y − x · z.  [left distribute −]
    g. (y − z) · x = y · x − z · x.  [right distribute −]

Theorem (left cancel · in ℤ).  Let x, y, z : ℤ.

    If x · y = x · z and x ≠ 0, then y = z.

Proof.  Assume that x · y = x · z and x ≠ 0.  Then x · y − x · z = 0 by [subtract to 0 iff equal].  Hence x · (y − z) = 0 by [left distribute −].  Since x ≠ 0, by [zero product] we have y − z = 0.  Then y = z by [subtract to 0 iff equal].

# Ordering

Axiom.  There is a relation < : ℤ → ℤ → 𝔹 such that for all a, b, c, d ∈ ℕ,

    z(a, b) < z(c, d) if and only if a + d < b + c.     [int_lt_def]

Definition.  For all i, j ∈ ℤ, i ≤ j iff i < j or i = j.

Definition.  For all i, j ∈ ℤ, we write i > j iff j < i.  We write i ≥ j iff j ≤ i.

Lemma.  For all n, j : ℕ, z(n, j) > 0 iff j < n.

Theorem.  Let a, b : ℤ.

    a. 0 : ℤ ≯ 0.  [0 not > 0]
    b. a > 0 or a = 0 or −a > 0. [positive trichotomy]
    c. If a > 0 and b > 0 then a + b > 0.  [sum of positives is positive]
    d. If a > 0 and b > 0 then a · b > 0.  [product of positives is positive]

Proof.

    b. There are some n, j : ℕ such that a = z(n, j).  By [trichotomy] we know that j < n or j = n or n < j.  If j < n then a > 0.  If j = n then a = 0.  We know that −a = z(j, n).  If n < j then z(j, n) > 0, so −a > 0.  In any case a > 0 or a = 0 or −a > 0.

    c. Suppose that a > 0 and b > 0.  There are some n, j, k, i : ℕ such that a = z(n, j) and b = z(k, i). Then z(n, j) > 0, so j < n.  Similarly z(k, i) > 0, so i < k.  Now a + b = z(n + k, j + i).  By [< add left right] we know that j + i < n + k.  Hence z(n + k, j + i) > 0, so a + b > 0.

    d. Suppose that a > 0 and b > 0.  There are some n, j, k, i : ℕ such that a = z(n, j) and b = z(k, i). Then z(n, j) > 0, so j < n.  Similarly z(k, i) > 0, so i < k.
    
    We know that k = i + s(u) for some u : ℕ.  Since j < n, by [< multiply right] we have j · s(u) < n · s(u).  Hence

        n · i + j · k = n · i + j · (i + s(u))
                         = n · i + j · i + j · s(u)
                         < n · i + j · i + n · s(u) 
                         = n · i + n · s(u) + j · i
                         = n · (i + s(u)) + j · i
                         = n · k + j · i.

    Then z(n, j) · z(k, i) > 0, so a · b > 0.

Theorem.  Let a, b : ℤ.  a < b if and only if b − a > 0.

Proof.  There are some n, j, k, i : ℕ such that a = z(n, j) and b = z(k, i).  So −a = z(j, n).  Then b − a = b + (−a) = z(k, i) + z(j, n) = z(k + j, i + n).

If a < b then z(n, j) < z(k, i), so n + i < j + k, so b − a > 0.  If b − a > 0 then n + i < j + k, so z(n, j) < z(k, i), so a < b.

# Axioms of an ordered integral domain

Theorem.  Let x, y, z : ℤ.

  a. x ≮ x.  [not less than self]
  b. If x < y and y < z then x < z.  [< is transitive on ℤ]
  c. x < y or x = y or y < x.  [weak trichotomy]
  d. If x < y then x + z < y + z. [< add right on ℤ]
  e. If x < y and 0 < z, then x · z < y · z. [< mul right on ℤ]
  f. 0 : ℤ ≠ 1.

Proof.

  a. Suppose that x < x.  Then x − x > 0.  But x − x = 0, so 0 : ℤ > 0, which is a contradiction to [0 not > 0].  Hence x ≮ x.

  b. Suppose that x < y and y < z.  Then y − x > 0 and z − y > 0.  Hence by [sum of positives is positive] (y − x) + (z − y) > 0.  But (y − x) + (z − y) = (z − x) + (y − y) = z − x.  Thus z − x > 0, so x < z.

  c. By [positive trichotomy] y − x > 0 or y − x = 0 or −(y − x) > 0.  If y − x > 0, then x < y.  If y − x = 0, then x = y.  If −(y − x) > 0, then x − y > 0, so y < x.  In any case x < y or x = y or y < x.

  d. Suppose that x < y.  Then y − x > 0.  But (y + z) − (x + z) = y − x.  So (y + z) − (x + z) > 0, hence x + z < y + z.

  e. Suppose that x < y and 0 < z.  Then y − x > 0 and z − 0 > 0.  But z − 0 = z, so z > 0.  By [product of positives is positive] (y − x) · z > 0.  But (y − x) · z = y · z − x · z by [right distribute −].  Hence y · z − x · z > 0, so x · z < y · z.

  f. 0 : ℕ ≠ 1, so 0 : ℕ + 0 ≠ 0 + 1, so z(0, 0) ≠ z(1, 0), so 0 : ℤ ≠ 1.

# Theorems that hold in any ordered integral domain

Theorem.  Let x, y, z : ℤ.

  a. If x < y and y ≤ z, then x < z.  [< with ≤ are transitive on ℤ]
  b. If x ≤ y and y < z, then x < z.  [≤ with < are transitive on ℤ]
  c. If x ≤ y and y ≤ z, then x ≤ z.  [≤ is transitive on ℤ]

Proof.

  a. Suppose that x < y and y ≤ z.  We know that y = z or y < z.  If y = z, then x < z.  If y < z, then x < z by [< is transitive on ℤ].

  b. Suppose that x ≤ y and y < z.  We know that x = y or x < y.  If x = y, then y < z.  If x < y, then x < z by [< is transitive on ℤ].

  c. Suppose that x ≤ y and y ≤ z.  We know that x = y or x < y.  If x = y, then x ≤ z.  If x < y, then by [< with ≤ are transitive on ℤ] x < z, so x ≤ z.

Theorem.  Let x, y, z, u, v : ℤ.

  a. Exactly one of the conditions x < y or x = y or y < x holds. [trichotomy on ℤ]
  b. If x < y and u < v then x + u < y + v.  [< add left right on ℤ]
  c. If x < y and u ≤ v then x + u < y + v.
  d. If x ≤ y and u < v then x + u < y + v.
  e. If x ≤ y and u ≤ v then x + u ≤ y + v. [≤ add left right on ℤ]
  f. If 0 < z and x · z < y · z then x < y.
  g. If x ≤ y + z then x − y ≤ z.

Proof.

  a. By [weak trichotomy] we have x < y or x = y or y < x.  If x < y and x = y then x < x, contradicting [not less than self].  If x = y and y < x then x < x, contradicting [not less than self].  If x < y and y < x then by [< is transitive on ℤ] we have x < x, again contradicting [not less than self].

  b. Assume that x < y and u < v.  Since x < y we have x + u < y + u by [< add right on ℤ].  Since u < v we have y + u < y + v by [< add right on ℤ].  Hence by [< is transitive on ℤ] we have x + u < y + v.

  d. Suppose that x ≤ y and u < v.  If x < y then x + u < y + v by [< add left right on ℤ].  If x = y then x + u < y + v by [< add right on ℤ].

  f. Suppose that 0 < z and x · z < y · z.  Also suppose that x ≮ y.  Then y < x or y = x.  If y < x then y · z < x · z, a contradiction.  If y = x then y · z = x · z, a contradiction.

  g. Suppose that x ≤ y + z.  Then by [≤ add left right on ℤ] x + (−y) ≤ y + z + (−y) = z + (y − y) = z + 0, so x − y ≤ z.

Definition.  For all x : ℤ, x is positive iff 0 < x.  x is negative iff x < 0.

Theorem.  Let x, y, z : ℤ.

  a. x < y iff y − x is positive.   [> iff difference positive]
  b. x < y iff x − y is negative.  [< iff difference negative]
  c. y is positive iff −y is negative.  [positive iff inverse negative]
  d. x is negative iff −x is positive.  [negative iff inverse positive]
  e. x < y iff −y < −x. [less iff negative less]
  f. If x < y and z < 0 then y · z < x · z.
  g. If x > 0 and y > 0 then x + y > 0 and x · y > 0. [sum and product positive]
  h. If x < 0 and y < 0 then x · y > 0.  [product of negatives is positive]
  i. If x > 0 and y < 0 then x · y < 0.  [product of positive and negative]
  j. If x ≠ 0 then x² > 0.  [square is positive]
  k. x² ≥ 0.
  l. −1 < 0 : ℤ < 1.

Proof.

  b. Suppose that x < y.  By [< add right on ℤ] x + (−y) < y + (−y), so x − y < 0.  Conversely, suppose that x − y < 0.  By [< add right on ℤ] (x − y) + y < 0 + y, so x < y.

  c. By [< iff difference negative] 0 < y iff 0 − y is negative.

  d. By [> iff difference positive] x < 0 iff 0 − x is positive.

  e. By [> iff difference positive] x < y iff 0 < y − x.  Also by [> iff difference positive]

      −y < −x iff 0 < (−x) − (−y).

  But (−x) − (−y) = (−x) + (−(−y)) = (−x) + y = y − x.

  f. Suppose that x < y and z < 0.  By [negative iff inverse positive] 0 < −z.  Hence by [< mul right on ℤ] x · (−z) < y · (−z).  But by [multiply by negative] x · (−z) = −(x · z) and y · (−z) = −(y · z).  Hence −(x · z) < −(y · z).  Therefore by [less iff negative less] y · z < x · z.

  g. Suppose that 0 < x and 0 < y.  By [< add left right on ℤ] 0 + 0 < x + y.  Also by [< mul right on ℤ] 0 · y < x · y.  But 0 · y = 0.  So x + y > 0 and x · y > 0.

  h. Suppose that x < 0 and y < 0.  Then by [negative iff inverse positive] 0 < −x and 0 < −y.  Hence by [sum and product positive] 0 < (−x) · (−y).  But (−x) · (−y) = x · y by [mul neg neg], so x · y > 0.

  i. Suppose that x > 0 and y < 0.  Then 0 < −y by [negative iff inverse positive].  Hence 0 < x · (−y) by [sum and product positive].  But x · (−y) = −(x · y) by [multiply by negative].  Hence 0 < −(x · y).  Then x · y < 0 by [negative iff inverse positive].

  j. Suppose that x ≠ 0.  Then x < 0 or 0 < x by [weak trichotomy].  If 0 < x then x² > 0 by [sum and product positive].  If x < 0 then x² > 0 by [product of negatives is positive].

  l. 1 ≠ 0 : ℤ, so by [square is positive] 0 < 1².  But 1² = 1.  Hence 0 : ℤ < 1.  By [positive iff inverse negative] −1 < 0.

# Absolute value

Definition.  For all x : ℤ, |x| = { x if 0 ≤ x;
                                               −x if x < 0 }.

Justification.  Let x : ℤ.  By [weak trichotomy] either 0 ≤ x or x < 0.  By [trichotomy on ℤ] it cannot be that 0 ≤ x and x < 0.

Theorem.  Let u, v, x, y, z : ℤ.

    a. 0 ≤ |x|.
    b. |x| = 0 if and only if x = 0.
    c. |−x| = |x|.  [abs of negative]
    d. |x − y| = |y − x|.  [abs difference]
    e. |x · y| = |x| · |y|.
    f. −|x| ≤ x ≤ |x|.  [abs range]
    g. |z| < u if and only if −u < z < u.  [abs range <]
    h. |z| ≤ u if and only if −u ≤ z ≤ u.  [abs range ≤]
    i. If u ≥ v and u ≥ −v then u ≥ |v|.  [greater than pos and neg]
    j. |x + y| ≤ |x| + |y|.   [triangle inequality]
    k. |x − y| ≥ | |x| − |y| |.

Proof.

    b. If x = 0 then |x| = x = 0.  If x ≠ 0 then by [weak trichotomy] 0 < x or x < 0.  If 0 < x then |x| = x > 0.  If x < 0 then |x| = −x > 0.

    c. By [trichotomy on ℤ] either 0 < x or 0 = x or x < 0.  If 0 < x then −x < 0, so |x| = x and |−x| = −(−x) = x.  If 0 = x then −x = 0 = x.  If x < 0 then −x > 0, so |x| = −x and |−x| = −x.

    d. x − y = −(y − x), so by [abs of negative] |x − y| = |y − x|.

    e. By [trichotomy on ℤ] either 0 < x or 0 = x or x < 0.  Also by [trichotomy on ℤ] either 0 < y or 0 = y or y < 0.

    Suppose that x = 0 or y = 0.  Then by [multiply by 0] x · y = 0, so |x · y| = 0.  Also |x| = 0 or |y| = 0, so |x| · |y| = 0, so |x · y| = |x| · |y|.

    Now suppose that 0 < x and 0 < y.  Then 0 < x · y by [sum and product positive].  Also |x · y| = x · y.  Also |x| = x and |y| = y, so |x · y| = |x| · |y|.

    Now suppose that x < 0 and y < 0.  Then 0 < x · y by [product of negatives is positive]. Hence |x · y| = x · y and |x| = −x and |y| = −y.  But (−x) · (−y) = x · y, so |x · y| = |x| · |y|.

    Now suppose that x < 0 and y > 0.  Then y · x < 0 by [product of positive and negative], so x · y < 0.  Hence |x · y|  = −(x · y) and |x| = −x and |y| = y.  But (−x) · y = −(x · y), so |x · y| = |x| · |y|.

    Finally, suppose that 0 < x and y < 0.  Then x · y < 0 by [product of positive and negative].  Hence |x · y|  = −(x · y) and |x| = x and |y| = −y.  But x · (−y) = −(x · y), so |x · y| = |x| · |y|.

    f. By [trichotomy on ℤ] either 0 < x or 0 = x or x < 0.  Suppose that 0 < x.  Then |x| = x.  Hence −|x| < 0.  By [< is transitive on ℤ] −|x| < x, thus −|x| < x = |x|.

    Now suppose that 0 = x.  Then −|x| = x = |x|.

    Finally, suppose that x < 0.  |x| = −x.  Hence −|x| = x and 0 < |x|.  By [< is transitive on ℤ] x < |x|, hence −|x| = x < |x|.

    g. Suppose that |z| < u.  By [abs range] z ≤ |z|.  Hence by [≤ with < are transitive on ℤ] z < u.  By [abs range] −|z| ≤ z.  But |z| < u, so −|z| > −u.  Hence by [< with ≤ are transitive on ℤ] −u < z, so −u < z < u.  Conversely, suppose that −u < z < u.  We know that either |z| = z or |z| = −z.  If |z| = z then |z| < u.  If |z| = −z then −u < −|z|, so |z| < u.  Hence |z| < u.

    h. Suppose that |z| ≤ u.  By [abs range] z ≤ |z|.  Hence by [≤ is transitive on ℤ] z ≤ u.  By [abs range] −|z| ≤ z.  But |z| ≤ u, so −|z| ≥ −u. Hence by [≤ is transitive on ℤ] −u ≤ z, so −u ≤ z ≤ u.  Conversely, suppose that −u ≤ z ≤ u.  We know that either |z| = z or |z| = −z.  If |z| = z then |z| ≤ u.  If |z| = −z then −u ≤ −|z|, so |z| ≤ u.  Hence |z| ≤ u.

    j. We know that −|x| ≤ x ≤ |x| and −|y| ≤ y ≤ |y|.  So

        (−|x|) + (−|y|) ≤ x + y ≤ |x| + |y|.

    Hence −(|x| + |y|) ≤ x + y ≤ |x| + |y|.  By [abs range ≤] we obtain |x + y| ≤ |x| + |y|.

   k. |x| = |y + (x − y)| ≤ |y| + |x − y| by [triangle inequality].  Therefore |x − y| ≥ |x| − |y|.  Similarly, |y| = |x + (y − x)| ≤ |x| + |y − x| by [triangle inequality].  Therefore |y − x| ≥ |y| − |x|.  But |x − y| = |y − x| by [abs difference].  Hence

        |x − y| ≥ |x| − |y|  and  |x − y| ≥ |y| − |x|.

    Notice that |y| − |x| = −(|x| − |y|).  Hence by [greater than pos and neg] we obtain |x − y| ≥ | |x| − |y| |.
